# Core dependencies for Brynhild
# Python >= 3.11 required

# LLM Provider clients
anthropic>=0.40.0          # Direct Anthropic API
openai>=1.0.0              # OpenRouter uses OpenAI-compatible API

# CLI framework
click>=8.0

# Terminal UI
rich>=13.0
textual>=0.50

# Async HTTP client
httpx>=0.27

# Data validation & settings
pydantic>=2.0
pydantic-settings>=2.0     # For environment variable loading

# File watching
watchfiles>=0.21

# Code parsing
tree-sitter>=0.21
tree-sitter-bash>=0.21

# YAML parsing (for plugin commands/agents)
pyyaml>=6.0

# Token counting for streaming display
tiktoken>=0.12.0

# Development dependencies
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.0            # Coverage reports
respx>=0.20                # Mock httpx requests
mypy>=1.8
black>=24.0
ruff>=0.3

